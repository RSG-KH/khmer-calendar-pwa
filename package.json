{
  "name": "khmer-calendar-pwa",
  "private": true,
  "version": "0.2.0",
  "appVersion": "0.2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node scripts/build-service-worker.mjs",
    "preview": "vite preview",
    "test": "npm run check:events && npm run build && node --test tests/*.test.mjs",
    "generate:events": "node scripts/generate-engine-event-dates.mjs",
    "check:events": "node scripts/generate-engine-event-dates.mjs --check"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "khmer-calendar-engine": "https://github.com/RSG-KH/khmer-calendar-engine/releases/download/v0.1.0/khmer-calendar-engine-0.1.0.tgz"
  }
}
